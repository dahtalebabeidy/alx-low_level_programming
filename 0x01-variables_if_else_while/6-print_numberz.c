#include <stdio.h>

/**
* main - Entry point
* Description: print digit from 0 to 9
* Return: always 0 (succes)
*/

int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
